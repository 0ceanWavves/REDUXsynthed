[build]
command = "npm run build"
publish = "dist"
NODE_VERSION = "18"

[build.environment]
NPM_FLAGS = "--version"

[site]
bucket = "./dist"
entry-point = "."

[[redirects]]
from = "/*"
to = "/index.html"
status = 200 