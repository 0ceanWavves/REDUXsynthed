---
// SplashTitle.astro - Component for the main title
---

<div class="splash-title flex flex-col items-center gap-1 pointer-events-none mt-6 md:mt-8 px-4" style="z-index: 999; position: relative;">
  <div class="gradient-text text-center font-extrabold tracking-tighter text-5xl md:text-7xl browser-optimized-text">
    <div class="title-word pointer-events-none" style="opacity: 1; transform: none;">Create</div>
    <div class="title-word pointer-events-none" style="opacity: 1; transform: none;">Design</div>
    <div class="title-word pointer-events-none" style="opacity: 1; transform: none;">Build</div>
  </div>
  <!-- Shadow elements for each word -->
  <div class="title-shadows absolute">
    <div class="title-shadow" data-index="0" style="opacity: 0.3; transform: scale(1);"></div>
    <div class="title-shadow" data-index="1" style="opacity: 0.3; transform: scale(1);"></div>
    <div class="title-shadow" data-index="2" style="opacity: 0.3; transform: scale(1);"></div>
  </div>
</div>

<style>
  /* Add position and higher z-index */
  .splash-title {
    position: relative; /* Ensure stacking context */
    z-index: 999;     /* Bring it way above other elements */
  }

  /* Base styling for gradient text */
  .gradient-text {
    background: linear-gradient(45deg, #8162ff, #00e599);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-fill-color: transparent;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
    position: relative;
    z-index: 75; /* This z-index is relative to .splash-title now */
  }

  /* Shadow elements for added depth effect */
  .title-shadows {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    z-index: -1; /* Keep shadows behind text */
  }

  .title-shadow {
    position: absolute;
    width: 100%;
    height: 12px;
    background: radial-gradient(ellipse at center, rgba(0,0,0,0.2) 0%, rgba(0,0,0,0) 70%);
    border-radius: 50%;
    left: 0;
    opacity: 0.3;
    transform: scale(1);
    transform-origin: center;
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  /* Position shadows beneath each word */
  .title-shadow[data-index="0"] {
    top: calc(33% - 6px);
  }
  .title-shadow[data-index="1"] {
    top: calc(66% - 6px);
  }
  .title-shadow[data-index="2"] {
    top: calc(100% - 6px);
  }

  /* Browser optimizations */
  .browser-optimized-text {
    /* Force proper stacking context in all browsers */
    transform: translateZ(0);
    will-change: transform, opacity;

    /* Prevent flickering */
    backface-visibility: hidden;

    /* Improve text rendering */
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* Firefox-specific optimizations - commented out properly */
  /*
  @-moz-document url-prefix() {
    .browser-optimized-text {
      transform: none !important;
      filter: none !important;
    }

    .gradient-text {
      background: linear-gradient(45deg, #8162ff, #00e599);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      color: #8162ff;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    }
  }
  */

  /* Chrome-specific fixes - commented out properly */
  /*
  @media screen and (-webkit-min-device-pixel-ratio: 0) and (min-resolution: 0.001dpcm) {
    .browser-optimized-text {
      transform: translateZ(0);
      z-index: 30 !important;
      position: relative;
    }

    .gradient-text {
      filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.5));
    }

    .title-word {
      text-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    }
  }
  */

  /* Mobile specific adjustments */
  @media (max-width: 768px) {
    /* Brighten text for better visibility on mobile */
    .gradient-text {
      filter: drop-shadow(0 6px 10px rgba(0, 0, 0, 0.5));
    }
  }

  /* Force title words to be visible */
  .title-word {
    opacity: 1 !important;
    transform: none !important;
    position: relative;
    display: block;
    pointer-events: none;
  }

  /* Add shake animation keyframes */
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    10%, 30%, 50%, 70% { transform: translateX(-2px); }
    20%, 40%, 60% { transform: translateX(2px); }
    80% { transform: translateX(1px); }
    90% { transform: translateX(-1px); }
  }

  .shake {
    animation: shake 0.4s cubic-bezier(0.36, 0.07, 0.19, 0.97) both;
  }

  @media (prefers-reduced-motion: reduce) {
    .title-word {
      opacity: 1 !important;
      transform: none !important;
      animation: none !important;
    }

    .title-shadow {
      display: none;
    }

    .gradient-text {
      animation: none;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    console.log("SplashTitle: DOMContentLoaded event fired");
    
    // Ensure title words are visible regardless of animation state
    const titleWords = document.querySelectorAll('.title-word');
    titleWords.forEach((word, index) => {
      if (word instanceof HTMLElement) {
        word.style.opacity = '1';
        word.style.transform = 'none';
      }
    });
    
    // Ensure shadows are visible
    const titleShadows = document.querySelectorAll('.title-shadow');
    titleShadows.forEach((shadow, index) => {
      if (shadow instanceof HTMLElement) {
        shadow.style.opacity = '0.3';
        shadow.style.transform = 'scale(1)';
      }
    });
  });
</script>